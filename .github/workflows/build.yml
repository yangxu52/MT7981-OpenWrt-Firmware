name: Build OpenWrt Firmware

on:
  workflow_dispatch:
    inputs:
      profile:
        description: Device profile
        required: true
        type: choice
        options:
          - h3c-nx30pro
          - cmcc-rax3000m

      upload_artifact:
        description: Upload firmware artifact
        required: true
        type: boolean
        default: true

      create_release:
        description: Create GitHub release
        required: true
        type: boolean
        default: false

env:
  REPO_URL: https://github.com/yangxu52/immortalwrt-mt798x
  REPO_BRANCH: openwrt-21.02
  TZ: Asia/Shanghai

jobs:
  build:
    name: Build ${{ inputs.profile }}
    runs-on: ubuntu-24.04

    permissions:
      contents: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v6

      - name: Load Profile
        shell: bash
        run: |
          echo "PROFILE_ID=${{ inputs.profile }}" >> "$GITHUB_ENV"
          bash scripts/ci/env.sh load-profile "${{ inputs.profile }}"

      - name: Cleanup Runner
        shell: bash
        run: |
          df -hT
          bash scripts/ci/cleanup.sh runner
          df -hT

      - name: Initialize Build Environment
        env: 
          DEBIAN_FRONTEND: noninteractive
        shell: bash
        run: |
          bash scripts/ci/build.sh init

      - name: Prepare Source Tree
        shell: bash
        run: |
          bash scripts/ci/build.sh prepare-source

      - name: Restore Toolchain Cache
        uses: actions/cache@v5
        with:
          path: |
            openwrt/staging_dir/host*
            openwrt/staging_dir/tool*
          key: toolchain-${{ hashFiles('openwrt/tools/**', 'openwrt/toolchain/**') }}

      - name: Restore Ccache
        uses: actions/cache@v5
        with:
          path: openwrt/.ccache
          key: ccache-${{ env.CACHE_NAME }}-${{ hashFiles(format('profiles/{0}/.config', inputs.profile)) }}
          restore-keys: |
            ccache-${{ env.CACHE_NAME }}-
            ccache-

      - name: Normalize Cache Directories
        if: ${{ !cancelled() }}
        shell: bash
        run: |
          mkdir -p openwrt/.ccache
          find openwrt/staging_dir -type d -exec chmod u+rwx {} + 2>/dev/null || true
          ccache -M 2G || true
          ccache -s || true

      - name: Apply Profile
        shell: bash
        run: |
          bash scripts/ci/build.sh apply-profile

      - name: Download Dependencies
        shell: bash
        run: |
          bash scripts/ci/build.sh download

      - name: Compile Firmware
        shell: bash
        run: |
          bash scripts/ci/build.sh compile

      - name: Show Disk Usage After Compile
        if: ${{ !cancelled() }}
        run: df -hT

      - name: Collect Artifacts
        if: ${{ !cancelled() }}
        shell: bash
        run: |
          bash scripts/ci/collect.sh

      - name: Upload Firmware Artifact
        if: ${{ success() && inputs.upload_artifact }}
        uses: actions/upload-artifact@v7
        with:
          name: ${{ env.ARTIFACT_NAME }}
          path: ${{ env.FIRMWARE_DIR }}

      - name: Prepare Release Metadata
        if: ${{ success() && inputs.create_release }}
        shell: bash
        run: |
          bash scripts/ci/build.sh prepare-release

      - name: Publish Release
        if: ${{ success() && inputs.create_release }}
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          name: ${{ env.RELEASE_NAME }}
          body_path: ${{ env.RELEASE_BODY }}
          files: ${{ env.FIRMWARE_DIR }}/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Delete Old Releases
        if: ${{ success() && inputs.create_release }}
        uses: dev-drprasad/delete-older-releases@master
        with:
          keep_latest: 7
          delete_tags: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Delete Old Workflow Runs
        if: ${{ !cancelled() }}
        uses: Mattraks/delete-workflow-runs@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          repository: ${{ github.repository }}
          retain_days: 1
          keep_minimum_runs: 3
